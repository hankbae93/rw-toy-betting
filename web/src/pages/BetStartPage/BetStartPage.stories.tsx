import type { Meta, StoryObj } from '@storybook/react'

import BetStartPage from './BetStartPage'

const meta: Meta<typeof BetStartPage> = {
  component: BetStartPage,
}

export default meta

type Story = StoryObj<typeof BetStartPage>

export const Primary: Story = {}
